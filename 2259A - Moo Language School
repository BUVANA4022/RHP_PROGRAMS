#include <iostream>
#include<vector>
using namespace std;
int main() {
    int u;
    cin>>u;
    for(int i=0;i<u;i++){
    int n,k;
    cin>>n>>k;
    string a;
    cin>>a;
    vector<bool> o(n/k,false);
    for(int i=0;i<a.length();i++){
        if( a[i] == '0')
            o[i/k]=true;
    }
    int cnt=0;
    for(int i=0;i<n/k;i++){
        if(!o[i])
            cnt++;
    }
    cout<<cnt<<endl;
    }
}
